{
  "name": "validity-compose",
  "description": "Validate using all the validators in a group.",
  "version": "1.0.0",
  "main": "validator.js",
  "scripts": {
    "lint": "eslint -f unix .",
    "pretest": "npm run lint",
    "test": "istanbul cover _mocha test",
    "posttest": "istanbul check-coverage && rm -rf coverage",
    "prepublish": "npm prune && npm test"
  },
  "publishConfig": {
    "registry": "http://registry.npmjs.org"
  },
  "author": "Paul Serb <paul@serby.net>",
  "license": "ISC",
  "dependencies": {
    "async": "^1.4.2"
  },
  "devDependencies": {
    "eslint": "^1.3.1",
    "istanbul": "^0.3.18",
    "mocha": "^2.2.5",
    "validity": "^0.0.3"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:serby/validity-compose"
  }
}
